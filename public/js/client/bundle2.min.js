function ajax_(url_, data_, success, loading) {
    $.ajax({
        type: "POST", url: url_, data: data_, beforeSend: function () {
            $(success).html(loading)
        }, success: function (html) {
            $(success).html(html)
        }
    })
}

function countProperty(obj) {
    var count = 0;
    for (var k in obj) obj.hasOwnProperty(k) && ++count;
    return count
}

function animateTo($el) {
    $("html, body").animate({scrollTop: $el.offset().top - 110}, "fast")
}

function isEmpty(value, allowEmptyString) {
    return null === value || "null" === value || void 0 === value || "undefined" === value || (allowEmptyString ? !1 : "" === value) || "object" == typeof value && 0 === countProperty(value) || value instanceof jQuery && 0 === value.length
}

function initAnimateArticle() {
    $().jCarouselLite && $(".animateArticle").jCarouselLite({
        btnNext: ".slideArticleContent .next",
        btnPrev: ".slideArticleContent .prev",
        mouseWheel: !0,
        visible: 1,
        afterEnd: function () {
            $(window).resize()
        }
    })
}

var getUrlParameter = function (sParam) {
    var sParameterName, i, sPageURL = decodeURIComponent(window.location.search.substring(1)),
        sURLVariables = sPageURL.split("&");
    for (i = 0; i < sURLVariables.length; i++) if (sParameterName = sURLVariables[i].split("="), sParameterName[0] === sParam) return void 0 === sParameterName[1] ? !0 : sParameterName[1]
}, validateObj = {
    check: function (obj, err_id, func) {
        return null == err_id && (err_id = "error-" + obj.name), $("#" + err_id).css("display", "none"), this[func](obj) === !1 ? ($("#" + err_id).css("display", "block"), !1) : !0
    }, ltrim: function (_1) {
        var _2 = /^\s */;
        return _1.replace(_2, "")
    }, rtrim: function (_3) {
        var _4 = /\s *$/;
        return _3.replace(_4, "")
    }, trim: function (_5) {
        return this.ltrim(this.rtrim(_5))
    }, _checkRequired: function (obj) {
        var st = this.trim(obj.value);
        return !("" === st || st === $(obj).attr("placeholder"))
    }, checkRequired: function (obj, err_id) {
        return this.check(obj, err_id, "_checkRequired")
    }, _checkEmail: function (obj) {
        var _2a = this.trim(obj.value), objRegExp = /\S+@\S+\.\S+/;
        return objRegExp.test(_2a)
    }, checkEmail: function (obj, err_id) {
        return this.check(obj, err_id, "_checkEmail")
    }, _checkPhone: function (obj) {
        var _40 = "0123456789-+() ", _41 = this.trim(obj.value);
        if (_41.length < 1) return !1;
        for (i = 0; i < _41.length; i++) if (ch = _41.charAt(i), _40.indexOf(ch) < 0) return !1;
        return !0
    }, checkPhone: function (obj, err_id) {
        return this.check(obj, err_id, "_checkPhone")
    }, _checkInt: function (obj) {
        var _2a = this.trim(obj.value);
        if (_2a.length < 1 || "" == _2a) return !1;
        var _2b = "0123456789";
        for (i = 0; i < _2a.length; i++) if (ch = _2a.charAt(i), _2b.indexOf(ch) < 0) return !1;
        return _2a.valueOf() <= 0 ? !1 : !0
    }, checkInt: function (obj, err_id) {
        return this.check(obj, err_id, "_checkInt")
    }, _checkDate: function (obj) {
        var _2a = this.trim(obj.value), objRegExp = /^\d{1,2}(\-|\/|\.)\d{1,2}\1\d{4}$/;
        return objRegExp.test(_2a)
    }, checkDate: function (obj, err_id) {
        return this.check(obj, err_id, "_checkDate")
    }, _checkUsername: function (obj) {
        var _2a = this.trim(obj.value), objRegExp = /^[a-zA-Z]+[a-zA-Z0-9]+$/;
        return objRegExp.test(_2a)
    }, checkUsername: function (obj, err_id) {
        return this.check(obj, err_id, "_checkUsername")
    }, _checkChecked: function (obj) {
        if (void 0 === obj.length) return obj.checked;
        var result = !1;
        for (i = 0; i < obj.length; i++) obj[i].checked && (result = !0);
        return result
    }, checkChecked: function (obj, err_id) {
        return this.check(obj, err_id, "_checkChecked")
    }, _checkRemote: function (obj) {
        var _2a = this.trim(obj.value), data = {}, result = !0;
        return data[obj.name] = _2a, $.ajax({
            type: "POST",
            data: data,
            url: obj["ajax-url"],
            async: !1,
            success: function (data) {
                1 !== parseInt(data) && (result = !1)
            }
        }), result
    }, checkRemote: function (obj, err_id, url) {
        return isEmpty(url) === !0 ? !1 : (obj["ajax-url"] = url, this.check(obj, err_id, "_checkRemote"))
    }
};
$(document).ready(function () {
    function checkWidth() {
        var windowsize = $window.width();
        windowsize > 1251 ? !function (e, n) {
            var o = e();
            e.fn.dropdownHover = function (t) {
                return "ontouchstart" in document ? this : (o = o.add(this.parent()), this.each(function () {
                    function r() {
                        n.clearTimeout(a), n.clearTimeout(i), i = n.setTimeout(function () {
                            o.find(":focus").blur(), f.instantlyCloseOthers === !0 && o.removeClass("open"), n.clearTimeout(i), d.attr("aria-expanded", "true"), s.addClass("open"), d.trigger(l)
                        }, f.hoverDelay)
                    }

                    var a, i, d = e(this), s = d.parent(), u = {delay: 300, hoverDelay: 0, instantlyCloseOthers: !0},
                        h = {
                            delay: e(this).data("delay"),
                            hoverDelay: e(this).data("hover-delay"),
                            instantlyCloseOthers: e(this).data("close-others")
                        }, l = "show.bs.dropdown", c = "hide.bs.dropdown", f = e.extend(!0, {}, u, t, h);
                    s.hover(function (e) {
                        return s.hasClass("open") || d.is(e.target) ? void r(e) : !0
                    }, function () {
                        n.clearTimeout(i), a = n.setTimeout(function () {
                            d.attr("aria-expanded", "false"), s.removeClass("open"), d.trigger(c)
                        }, f.delay)
                    }), d.hover(function (e) {
                        return s.hasClass("open") || s.is(e.target) ? void r(e) : !0
                    }), s.find(".dropdown-submenu").each(function () {
                        var o, t = e(this);
                        t.hover(function () {
                            n.clearTimeout(o), t.children(".dropdown-menu").show(), t.siblings().children(".dropdown-menu").hide()
                        }, function () {
                            var e = t.children(".dropdown-menu");
                            o = n.setTimeout(function () {
                                e.hide()
                            }, f.delay)
                        })
                    })
                }))
            }, e(document).ready(function () {
                e('[data-hover="dropdown"]').dropdownHover()
            })
        }(jQuery, window) : $(".dropdown-toggle").mouseover(function () {
            return !1
        })
    }

    var $window = $(window);
    checkWidth(), $(window).resize(function () {
        checkWidth()
    })
}), $(document).ready(function () {
    $("#mobile-navigation-menu-close").click(function (e) {
        e.preventDefault(), $("#main-navigation-wrapper").toggleClass("active")
    }), $("#mobile-navigation-menu-toggle").click(function (e) {
        e.preventDefault(), $("#main-navigation-wrapper").toggleClass("active")
    })
}), $(document).ready(function () {
    "use strict";
    var refreshOnResize = function () {
        window.innerWidth < "1024" ? ($(".container-fluid.page").css("margin-top", $(".logo").height() + "px"), $(".main-menu").css("top", $(".logo").height() + "px")) : ($(".container-fluid.page").css("margin-top", "70px"), $(".main-menu").css("top", "70px"))
    };
    refreshOnResize(), $(window).resize(refreshOnResize), $('[data-toggle="tooltip"]').tooltip()
}), $(document).ready(function () {
    function isScrolledTo(elem) {
        var docViewTop = $(window).scrollTop();
        docViewTop + $(window).height();
        if ($("#sticky").length) var elemTop = $(elem).offset().top;
        elemTop + $(elem).height();
        return docViewTop >= elemTop || elemTop >= docViewTop
    }

    var catcher = $(".catcher"), sticky = $("#sticky"), footer = $("#footer");
    if ($("#footer").length) var footTop = footer.offset().top;
    if ($("#sticky").length) {
        sticky.offset().top
    }
    $(window).scroll(function () {
        isScrolledTo(sticky) && (sticky.css("position", "fixed"), sticky.css("top", "2px"), sticky.css("margin-top", "110px"));
        var stopHeight = catcher.offset().top + catcher.height(), stickyFoot = sticky.offset().top + sticky.height();
        stickyFoot > footTop + 190 ? (console.log(stickyFoot), sticky.css({
            position: "absolute",
            top: footTop - 20 - sticky.height()
        }), sticky.css("margin-top", "0px")) : sticky && sticky.offset() && stopHeight > sticky.offset().top && (sticky.css("position", "relative"), sticky.css("top", "inherit"), sticky.css("margin-top", "0px"))
    })
}), $(document).ready(function () {
    function setCookie(cname, cvalue, exdays) {
        var d = new Date;
        d.setTime(d.getTime() + 24 * exdays * 60 * 60 * 1e3);
        var expires = "expires=" + d.toUTCString();
        document.cookie = cname + "=" + cvalue + "; " + expires
    }

    function getCookie(cname) {
        for (var name = cname + "=", ca = document.cookie.split(";"), i = 0; i < ca.length; i++) {
            for (var c = ca[i]; " " == c.charAt(0);) c = c.substring(1);
            if (-1 != c.indexOf(name)) return c.substring(name.length, c.length)
        }
        return ""
    }

    var bannerHomepageCookie = "banner-home-page", isAllowPopup = 0;
    isEmpty(getCookie(bannerHomepageCookie)) === !0 && isAllowPopup === !0 && setTimeout(function () {
        var $bannerHomepageContainer = $("." + bannerHomepageCookie);
        isEmpty($bannerHomepageContainer) === !1 && isEmpty($bannerHomepageContainer.find(".holder a")) === !1 && ($bannerHomepageContainer.show().find(".background, .button-close").click(function (event) {
            $(this).closest(".banner-home-page").remove()
        }), setCookie(bannerHomepageCookie, 1, .5))
    }, 5e3)
});